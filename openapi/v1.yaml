openapi: 3.0.3
info:
  title: ShiftLush API
  version: v1
paths:
  /api/v1/attachments/:
    get:
      operationId: attachments_list
      description: |-
        Files, without the bytes.

        There is no update: a file is not edited, it is replaced. Correcting one
        means uploading the new version and deleting the old, which leaves both in
        the audit trail — where a silent overwrite would leave neither.
      parameters:
      - in: query
        name: category
        schema:
          type: string
          enum:
          - ce_certificate
          - declaration_of_conformity
          - inspection_report
          - logo
          - other
          - permit
          - photo
          - signed_contract
        description: |-
          * `photo` - Photo
          * `ce_certificate` - CE certificate
          * `declaration_of_conformity` - Declaration of conformity
          * `permit` - Permit
          * `signed_contract` - Signed contract
          * `inspection_report` - Inspection report
          * `logo` - Logo
          * `other` - Other
      - in: query
        name: object_id
        schema:
          type: string
          format: uuid
      - in: query
        name: object_type
        schema:
          type: string
          enum:
          - building
          - company
          - contract
          - customer
          - elevator
          - user
        description: |-
          * `elevator` - Elevator
          * `building` - Building
          * `contract` - Contract
          * `customer` - Customer
          * `company` - Company
          * `user` - User
      - name: ordering
        required: false
        in: query
        description: Which field to use when ordering the results.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: A page number within the paginated result set.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Number of results to return per page.
        schema:
          type: integer
      tags:
      - attachments
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAttachmentList'
          description: ''
    post:
      operationId: attachments_create
      description: |-
        Confirm an upload that has already landed.

        Retrying is safe: a storage key names one object, so a second
        confirmation returns the record the first one made.
      tags:
      - attachments
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttachmentConfirmRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AttachmentConfirmRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AttachmentConfirmRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Attachment'
          description: ''
  /api/v1/attachments/{id}/:
    get:
      operationId: attachments_retrieve
      description: |-
        Files, without the bytes.

        There is no update: a file is not edited, it is replaced. Correcting one
        means uploading the new version and deleting the old, which leaves both in
        the audit trail — where a silent overwrite would leave neither.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this attachment.
        required: true
      tags:
      - attachments
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Attachment'
          description: ''
    delete:
      operationId: attachments_destroy
      description: |-
        Files, without the bytes.

        There is no update: a file is not edited, it is replaced. Correcting one
        means uploading the new version and deleting the old, which leaves both in
        the audit trail — where a silent overwrite would leave neither.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this attachment.
        required: true
      tags:
      - attachments
      security:
      - jwtAuth: []
      responses:
        '204':
          description: No response body
  /api/v1/attachments/{id}/download-url/:
    get:
      operationId: attachments_download_url_retrieve
      description: |-
        A URL that works for a few minutes and only for this file.

        The bucket is never public. Every read is a fresh signature, which is
        also why these URLs are not worth caching or embedding anywhere.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this attachment.
        required: true
      tags:
      - attachments
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DownloadUrl'
          description: ''
  /api/v1/attachments/upload-url/:
    post:
      operationId: attachments_upload_url_create
      description: |-
        Ask for permission to upload, then upload straight to storage.

        No record is created here. A row for a file that was never uploaded is
        worse than no row: it appears in lists, it cannot be downloaded, and
        nothing ever cleans it up.
      tags:
      - attachments
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UploadUrlRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/UploadUrlRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UploadUrlRequestRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UploadUrlResponse'
          description: ''
  /api/v1/audit-logs/:
    get:
      operationId: audit_logs_list
      description: |-
        Read-only, and only for owners and admins.

        There is no create, update or delete anywhere: a log that can be edited is
        not evidence. Writes reach this table through signals only.
      parameters:
      - in: query
        name: action
        schema:
          type: string
          enum:
          - create
          - delete
          - update
        description: |-
          * `create` - Create
          * `update` - Update
          * `delete` - Delete
      - name: ordering
        required: false
        in: query
        description: Which field to use when ordering the results.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: A page number within the paginated result set.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Number of results to return per page.
        schema:
          type: integer
      - in: query
        name: record_id
        schema:
          type: string
          format: uuid
      - in: query
        name: since
        schema:
          type: string
          format: date-time
      - in: query
        name: table_name
        schema:
          type: string
      - in: query
        name: until
        schema:
          type: string
          format: date-time
      - in: query
        name: user_id
        schema:
          type: string
          format: uuid
      tags:
      - audit-logs
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAuditLogList'
          description: ''
  /api/v1/auth/email/resend:
    post:
      operationId: auth_email_resend_create
      description: |-
        Send the verification link again.

        The only way out for somebody who mistyped their address at registration or
        lost the message. Answers the same way whether or not there was anything to
        send: an authenticated caller asking about their own address learns nothing
        either way, and a verified account should not be told it is verified by a
        different status code.
      tags:
      - auth
      security:
      - jwtAuth: []
      responses:
        '204':
          description: No response body
  /api/v1/auth/email/verify:
    post:
      operationId: auth_email_verify_create
      tags:
      - auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmailVerifyRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/EmailVerifyRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/EmailVerifyRequest'
        required: true
      security:
      - jwtAuth: []
      - {}
      responses:
        '204':
          description: No response body
  /api/v1/auth/login:
    post:
      operationId: auth_login_create
      tags:
      - auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LoginRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LoginRequest'
        required: true
      security:
      - jwtAuth: []
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
          description: ''
  /api/v1/auth/logout:
    post:
      operationId: auth_logout_create
      tags:
      - auth
      security:
      - jwtAuth: []
      - {}
      responses:
        '204':
          description: No response body
  /api/v1/auth/me:
    get:
      operationId: auth_me_retrieve
      tags:
      - auth
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CurrentUser'
          description: ''
  /api/v1/auth/password:
    post:
      operationId: auth_password_create
      description: 'Verifies the current password, applies the same password policy
        as registration, and ends every other session on the account. The session
        making the call survives: a new refresh cookie is set and a new access token
        is returned. A wrong current password answers 422 INVALID_CREDENTIALS, distinct
        from the 400 a policy failure gives.'
      summary: Change the signed-in user's password
      tags:
      - auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordChangeRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PasswordChangeRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PasswordChangeRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
          description: ''
  /api/v1/auth/password-reset:
    post:
      operationId: auth_password_reset_create
      tags:
      - auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordResetRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PasswordResetRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PasswordResetRequestRequest'
        required: true
      security:
      - jwtAuth: []
      - {}
      responses:
        '202':
          description: No response body
  /api/v1/auth/password-reset/confirm:
    post:
      operationId: auth_password_reset_confirm_create
      tags:
      - auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordResetConfirmRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PasswordResetConfirmRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PasswordResetConfirmRequest'
        required: true
      security:
      - jwtAuth: []
      - {}
      responses:
        '204':
          description: No response body
  /api/v1/auth/refresh:
    post:
      operationId: auth_refresh_create
      description: |-
        Exchanges the refresh cookie for a new access token.

        Open to unauthenticated callers by definition: the access token has expired,
        which is why the client is here.
      tags:
      - auth
      security:
      - jwtAuth: []
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
          description: ''
  /api/v1/auth/register:
    post:
      operationId: auth_register_create
      tags:
      - auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
        required: true
      security:
      - jwtAuth: []
      - {}
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
          description: ''
  /api/v1/auth/sessions:
    get:
      operationId: auth_sessions_list
      description: 'One entry per signed-in device, not per refresh token: refresh
        rotation replaces the stored token roughly every fifteen minutes and every
        replacement stays in the same session. `id` is stable for the life of the
        session and is what the revoke endpoint takes. Exactly one entry has `is_current:
        true` — the session this request arrived on — unless the request carried no
        refresh cookie, in which case none does.'
      summary: List the caller's own sessions
      tags:
      - auth
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Session'
          description: ''
  /api/v1/auth/sessions/{session_id}:
    delete:
      operationId: auth_sessions_destroy
      description: Ends the session and every refresh token in it. The refresh token
        that device holds is refused from that moment, and replaying it trips the
        existing reuse detection. Revoking the current session is allowed and is equivalent
        to signing out here — the refresh cookie is cleared in the response. A session
        id belonging to another user, or one that has already ended, answers 404.
      summary: End one of the caller's sessions
      parameters:
      - in: path
        name: session_id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - auth
      security:
      - jwtAuth: []
      responses:
        '204':
          description: No response body
  /api/v1/auth/sessions/revoke-others:
    post:
      operationId: auth_sessions_revoke_others_create
      description: The session this request arrives on is kept; every other session
        on the account ends immediately. If the request carries no usable refresh
        cookie there is no session to keep, and all of them end — including the caller's,
        who has to sign in again.
      summary: End every session except the caller's own
      tags:
      - auth
      security:
      - jwtAuth: []
      responses:
        '204':
          description: No response body
  /api/v1/buildings/:
    get:
      operationId: buildings_list
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: query
        name: complex
        schema:
          type: string
          format: uuid
      - in: query
        name: customer
        schema:
          type: string
          format: uuid
      - in: query
        name: is_active
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: Which field to use when ordering the results.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: A page number within the paginated result set.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Number of results to return per page.
        schema:
          type: integer
      - in: query
        name: search
        schema:
          type: string
      - in: query
        name: type
        schema:
          type: string
          enum:
          - commercial
          - hospital
          - hotel
          - industrial
          - mall
          - mixed_use
          - public
          - residential
          - school
        description: |-
          * `residential` - Residential
          * `commercial` - Commercial
          * `mixed_use` - Mixed use
          * `public` - Public
          * `hospital` - Hospital
          * `mall` - Mall
          * `hotel` - Hotel
          * `school` - School
          * `industrial` - Industrial
      tags:
      - buildings
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedBuildingReadList'
          description: ''
    post:
      operationId: buildings_create
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      tags:
      - buildings
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BuildingWriteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BuildingWriteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BuildingWriteRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BuildingWrite'
          description: ''
  /api/v1/buildings/{id}/:
    get:
      operationId: buildings_retrieve
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this building.
        required: true
      tags:
      - buildings
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BuildingRead'
          description: ''
    patch:
      operationId: buildings_partial_update
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this building.
        required: true
      tags:
      - buildings
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedBuildingWriteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedBuildingWriteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedBuildingWriteRequest'
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BuildingWrite'
          description: ''
    delete:
      operationId: buildings_destroy
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this building.
        required: true
      tags:
      - buildings
      security:
      - jwtAuth: []
      responses:
        '204':
          description: No response body
  /api/v1/company:
    get:
      operationId: company_retrieve
      tags:
      - company
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Company'
          description: ''
    patch:
      operationId: company_partial_update
      tags:
      - company
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedCompanyRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedCompanyRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedCompanyRequest'
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Company'
          description: ''
  /api/v1/complexes/:
    get:
      operationId: complexes_list
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: query
        name: customer
        schema:
          type: string
          format: uuid
      - name: ordering
        required: false
        in: query
        description: Which field to use when ordering the results.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: A page number within the paginated result set.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Number of results to return per page.
        schema:
          type: integer
      tags:
      - complexes
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedComplexReadList'
          description: ''
    post:
      operationId: complexes_create
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      tags:
      - complexes
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ComplexWriteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ComplexWriteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ComplexWriteRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComplexWrite'
          description: ''
  /api/v1/complexes/{id}/:
    get:
      operationId: complexes_retrieve
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this complex.
        required: true
      tags:
      - complexes
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComplexRead'
          description: ''
    patch:
      operationId: complexes_partial_update
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this complex.
        required: true
      tags:
      - complexes
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedComplexWriteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedComplexWriteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedComplexWriteRequest'
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComplexWrite'
          description: ''
    delete:
      operationId: complexes_destroy
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this complex.
        required: true
      tags:
      - complexes
      security:
      - jwtAuth: []
      responses:
        '204':
          description: No response body
  /api/v1/contracts/:
    get:
      operationId: contracts_list
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: query
        name: customer
        schema:
          type: string
          format: uuid
      - name: ordering
        required: false
        in: query
        description: Which field to use when ordering the results.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: A page number within the paginated result set.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Number of results to return per page.
        schema:
          type: integer
      - in: query
        name: scope
        schema:
          type: string
          enum:
          - full_coverage
          - maintenance_and_repair
          - maintenance_only
        description: |-
          * `maintenance_only` - Maintenance only
          * `maintenance_and_repair` - Maintenance and repair
          * `full_coverage` - Full coverage
      - in: query
        name: search
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          enum:
          - active
          - draft
          - expired
          - renewed
          - terminated
        description: |-
          * `draft` - Draft
          * `active` - Active
          * `expired` - Expired
          * `terminated` - Terminated
          * `renewed` - Renewed
      tags:
      - contracts
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedContractReadList'
          description: ''
    post:
      operationId: contracts_create
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      tags:
      - contracts
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContractWriteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ContractWriteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ContractWriteRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContractWrite'
          description: ''
  /api/v1/contracts/{id}/:
    get:
      operationId: contracts_retrieve
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this contract.
        required: true
      tags:
      - contracts
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContractRead'
          description: ''
    patch:
      operationId: contracts_partial_update
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this contract.
        required: true
      tags:
      - contracts
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedContractWriteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedContractWriteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedContractWriteRequest'
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContractWrite'
          description: ''
    delete:
      operationId: contracts_destroy
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this contract.
        required: true
      tags:
      - contracts
      security:
      - jwtAuth: []
      responses:
        '204':
          description: No response body
  /api/v1/contracts/{id}/elevators/:
    post:
      operationId: contracts_elevators_create
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this contract.
        required: true
      tags:
      - contracts
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddElevatorsRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AddElevatorsRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AddElevatorsRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContractRead'
          description: ''
  /api/v1/contracts/{id}/elevators/{elevator_id}/:
    delete:
      operationId: contracts_elevators_destroy
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: elevator_id
        schema:
          type: string
          format: uuid
        required: true
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this contract.
        required: true
      tags:
      - contracts
      security:
      - jwtAuth: []
      responses:
        '204':
          description: No response body
  /api/v1/contracts/{id}/renew/:
    post:
      operationId: contracts_renew_create
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this contract.
        required: true
      tags:
      - contracts
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RenewRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RenewRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RenewRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContractRead'
          description: ''
  /api/v1/contracts/{id}/terminate/:
    post:
      operationId: contracts_terminate_create
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this contract.
        required: true
      tags:
      - contracts
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TerminateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TerminateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TerminateRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContractRead'
          description: ''
  /api/v1/customer-contacts/:
    get:
      operationId: customer_contacts_list
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: query
        name: customer
        schema:
          type: string
          format: uuid
      - in: query
        name: is_primary
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: Which field to use when ordering the results.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: A page number within the paginated result set.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Number of results to return per page.
        schema:
          type: integer
      - in: query
        name: role
        schema:
          type: string
          enum:
          - accounting
          - auditor
          - caretaker
          - manager
          - other
          - technical_lead
        description: |-
          * `manager` - Manager
          * `auditor` - Auditor
          * `caretaker` - Caretaker
          * `technical_lead` - Technical lead
          * `accounting` - Accounting
          * `other` - Other
      tags:
      - customer-contacts
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedCustomerContactReadList'
          description: ''
    post:
      operationId: customer_contacts_create
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      tags:
      - customer-contacts
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CustomerContactWriteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CustomerContactWriteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CustomerContactWriteRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerContactWrite'
          description: ''
  /api/v1/customer-contacts/{id}/:
    get:
      operationId: customer_contacts_retrieve
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this customer contact.
        required: true
      tags:
      - customer-contacts
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerContactRead'
          description: ''
    patch:
      operationId: customer_contacts_partial_update
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this customer contact.
        required: true
      tags:
      - customer-contacts
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedCustomerContactWriteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedCustomerContactWriteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedCustomerContactWriteRequest'
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerContactWrite'
          description: ''
    delete:
      operationId: customer_contacts_destroy
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this customer contact.
        required: true
      tags:
      - customer-contacts
      security:
      - jwtAuth: []
      responses:
        '204':
          description: No response body
  /api/v1/customers/:
    get:
      operationId: customers_list
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: query
        name: is_active
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: Which field to use when ordering the results.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: A page number within the paginated result set.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Number of results to return per page.
        schema:
          type: integer
      - in: query
        name: search
        schema:
          type: string
      - in: query
        name: type
        schema:
          type: string
          enum:
          - building_management
          - complex_management
          - corporate
          - individual
          - public
        description: |-
          * `complex_management` - Complex management
          * `building_management` - Building management
          * `corporate` - Corporate
          * `public` - Public
          * `individual` - Individual
      tags:
      - customers
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedCustomerReadList'
          description: ''
    post:
      operationId: customers_create
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      tags:
      - customers
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CustomerWriteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CustomerWriteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CustomerWriteRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerWrite'
          description: ''
  /api/v1/customers/{id}/:
    get:
      operationId: customers_retrieve
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this customer.
        required: true
      tags:
      - customers
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerRead'
          description: ''
    patch:
      operationId: customers_partial_update
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this customer.
        required: true
      tags:
      - customers
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedCustomerWriteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedCustomerWriteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedCustomerWriteRequest'
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerWrite'
          description: ''
    delete:
      operationId: customers_destroy
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this customer.
        required: true
      tags:
      - customers
      security:
      - jwtAuth: []
      responses:
        '204':
          description: No response body
  /api/v1/customers/{id}/contacts/:
    get:
      operationId: customers_contacts_list
      description: The contacts of this customer.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this customer.
        required: true
      - name: page
        required: false
        in: query
        description: A page number within the paginated result set.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Number of results to return per page.
        schema:
          type: integer
      tags:
      - customers
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedCustomerContactReadList'
          description: ''
    post:
      operationId: customers_contacts_create
      description: Add a contact to this customer. The customer comes from the path.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this customer.
        required: true
      tags:
      - customers
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CustomerContactNestedWriteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CustomerContactNestedWriteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CustomerContactNestedWriteRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerContactRead'
          description: ''
  /api/v1/districts/:
    get:
      operationId: districts_list
      parameters:
      - name: ordering
        required: false
        in: query
        description: Which field to use when ordering the results.
        schema:
          type: string
      - in: query
        name: province
        schema:
          type: integer
        required: true
      tags:
      - districts
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/District'
          description: ''
  /api/v1/elevators/:
    get:
      operationId: elevators_list
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: query
        name: building
        schema:
          type: string
          format: uuid
      - in: query
        name: category
        schema:
          type: string
          enum:
          - accessibility_platform
          - dumbwaiter
          - freight
          - passenger
          - passenger_freight
          - vehicle
        description: |-
          * `passenger` - Passenger
          * `freight` - Freight
          * `passenger_freight` - Passenger and freight
          * `dumbwaiter` - Dumbwaiter
          * `accessibility_platform` - Accessibility platform
          * `vehicle` - Vehicle
      - in: query
        name: customer
        schema:
          type: string
          format: uuid
      - in: query
        name: inspection_label
        schema:
          type: string
          enum:
          - blue
          - green
          - none
          - red
          - yellow
        description: |-
          * `green` - Green
          * `blue` - Blue
          * `yellow` - Yellow
          * `red` - Red
          * `none` - None
      - name: ordering
        required: false
        in: query
        description: Which field to use when ordering the results.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: A page number within the paginated result set.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Number of results to return per page.
        schema:
          type: integer
      - in: query
        name: search
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          enum:
          - active
          - out_of_service
          - sealed
          - suspended
          - uncontracted
        description: |-
          * `active` - Active
          * `suspended` - Suspended
          * `sealed` - Sealed
          * `out_of_service` - Out of service
          * `uncontracted` - Uncontracted
      tags:
      - elevators
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedElevatorListList'
          description: ''
    post:
      operationId: elevators_create
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      tags:
      - elevators
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ElevatorWriteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ElevatorWriteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ElevatorWriteRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElevatorWrite'
          description: ''
  /api/v1/elevators/{id}/:
    get:
      operationId: elevators_retrieve
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this elevator.
        required: true
      tags:
      - elevators
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElevatorDetail'
          description: ''
    patch:
      operationId: elevators_partial_update
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this elevator.
        required: true
      tags:
      - elevators
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedElevatorWriteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedElevatorWriteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedElevatorWriteRequest'
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElevatorWrite'
          description: ''
    delete:
      operationId: elevators_destroy
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this elevator.
        required: true
      tags:
      - elevators
      security:
      - jwtAuth: []
      responses:
        '204':
          description: No response body
  /api/v1/elevators/{id}/regenerate-qr/:
    post:
      operationId: elevators_regenerate_qr_create
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this elevator.
        required: true
      tags:
      - elevators
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElevatorDetail'
          description: ''
  /api/v1/elevators/by-qr/{token}/:
    get:
      operationId: elevators_by_qr_retrieve
      description: |-
        CRUD over a company-owned model.

        PUT is not routed: partial update is the only update verb. A full replace
        means the client has to send every field it does not want cleared, and the
        moment the model grows a column, old clients start wiping it.
      parameters:
      - in: path
        name: token
        schema:
          type: string
        description: The twelve-character token printed on the label.
        required: true
      tags:
      - elevators
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElevatorByQr'
          description: ''
  /api/v1/elevators/labels/:
    post:
      operationId: elevators_labels_create
      description: 'A printable A4 sheet, twelve labels to the page. The identifiers
        are sent in the body rather than the query string: a firm printing its whole
        estate would otherwise build a URL no proxy will accept.'
      tags:
      - elevators
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LabelRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LabelRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LabelRequestRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/pdf:
              schema:
                type: string
                format: binary
          description: ''
  /api/v1/geocode/reverse:
    get:
      operationId: geocode_reverse_retrieve
      description: |-
        `GET /geocode/reverse?lat=&lng=` — a point, as ids from our own tables.

        The lookup goes through this API rather than from the browser so answers
        can be cached, the provider's quota stays under one roof, and changing
        provider is a change here (8.6). The throttle is the second half of that:
        caching bounds repeat lookups, but nothing bounds a client walking the
        coordinate space except a rate limit.

        A level that could not be resolved comes back null and named in
        `unmatched`. That is the whole point of the endpoint rather than a
        shortcoming of it — see `apps.address.matching`.
      summary: Resolve a map point to province, district and neighborhood ids
      parameters:
      - in: query
        name: lat
        schema:
          type: number
          format: double
          maximum: 90
          minimum: -90
        required: true
      - in: query
        name: lng
        schema:
          type: number
          format: double
          maximum: 180
          minimum: -180
        required: true
      tags:
      - geocode
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReverseGeocode'
          description: ''
  /api/v1/invitations/:
    get:
      operationId: invitations_list
      description: |-
        Pending and past invitations.

        Deleting one is how an invitation is revoked: the row is soft-deleted, and
        the token stops resolving because the lookup is scoped to live rows. A
        mistyped address should stop working the moment it is noticed.
      parameters:
      - name: ordering
        required: false
        in: query
        description: Which field to use when ordering the results.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: A page number within the paginated result set.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Number of results to return per page.
        schema:
          type: integer
      tags:
      - invitations
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedInvitationList'
          description: ''
    post:
      operationId: invitations_create
      description: |-
        Pending and past invitations.

        Deleting one is how an invitation is revoked: the row is soft-deleted, and
        the token stops resolving because the lookup is scoped to live rows. A
        mistyped address should stop working the moment it is noticed.
      tags:
      - invitations
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InvitationCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/InvitationCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/InvitationCreateRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Invitation'
          description: ''
  /api/v1/invitations/{id}/:
    delete:
      operationId: invitations_destroy
      description: |-
        Pending and past invitations.

        Deleting one is how an invitation is revoked: the row is soft-deleted, and
        the token stops resolving because the lookup is scoped to live rows. A
        mistyped address should stop working the moment it is noticed.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this invitation.
        required: true
      tags:
      - invitations
      security:
      - jwtAuth: []
      responses:
        '204':
          description: No response body
  /api/v1/invitations/{id}/resend/:
    post:
      operationId: invitations_resend_create
      description: |-
        Pending and past invitations.

        Deleting one is how an invitation is revoked: the row is soft-deleted, and
        the token stops resolving because the lookup is scoped to live rows. A
        mistyped address should stop working the moment it is noticed.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this invitation.
        required: true
      tags:
      - invitations
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Invitation'
          description: ''
  /api/v1/invitations/accept:
    post:
      operationId: invitations_accept_create
      tags:
      - invitations
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InvitationAcceptRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/InvitationAcceptRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/InvitationAcceptRequest'
        required: true
      security:
      - jwtAuth: []
      - {}
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
          description: ''
  /api/v1/invitations/verify/{token}:
    get:
      operationId: invitations_verify_retrieve
      description: |-
        What the sign-up screen reads before anyone has an account.

        Public by necessity: the invitee cannot authenticate yet. It answers only
        for a valid, unexpired, unaccepted token, and returns only what the e-mail
        they are holding already told them.
      parameters:
      - in: path
        name: token
        schema:
          type: string
        required: true
      tags:
      - invitations
      security:
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvitationPreview'
          description: ''
  /api/v1/neighborhoods/:
    get:
      operationId: neighborhoods_list
      description: |-
        Typeahead only. The full list is never served.

        Searching goes through `name_normalized`, which the loader fills with the
        same `normalize()` the query uses. They have to agree character for
        character — if they ever diverge, part of the table becomes unreachable and
        nothing raises.
      parameters:
      - in: query
        name: district
        schema:
          type: integer
        required: true
      - name: ordering
        required: false
        in: query
        description: Which field to use when ordering the results.
        schema:
          type: string
      - in: query
        name: search
        schema:
          type: string
        description: At least two characters.
      tags:
      - neighborhoods
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Neighborhood'
          description: ''
  /api/v1/provinces/:
    get:
      operationId: provinces_list
      description: All 81, unpaginated. Small enough to be a dropdown.
      parameters:
      - name: ordering
        required: false
        in: query
        description: Which field to use when ordering the results.
        schema:
          type: string
      tags:
      - provinces
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Province'
          description: ''
  /api/v1/users/:
    get:
      operationId: users_list
      description: |-
        Colleagues.

        There is no create: accounts come from invitations, because an
        administrator who can set a password can read one, and the specification
        rules that out for both security and data-protection reasons. There is no
        delete either — a leaver is deactivated, since their audit trail has to
        outlive their employment.
      parameters:
      - in: query
        name: is_active
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: Which field to use when ordering the results.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: A page number within the paginated result set.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Number of results to return per page.
        schema:
          type: integer
      - in: query
        name: role
        schema:
          type: string
          enum:
          - accountant
          - admin
          - operations
          - owner
          - technician
        description: |-
          * `owner` - Owner
          * `admin` - Admin
          * `operations` - Operations
          * `technician` - Technician
          * `accountant` - Accountant
      tags:
      - users
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedUserList'
          description: ''
  /api/v1/users/{id}/:
    get:
      operationId: users_retrieve
      description: |-
        Colleagues.

        There is no create: accounts come from invitations, because an
        administrator who can set a password can read one, and the specification
        rules that out for both security and data-protection reasons. There is no
        delete either — a leaver is deactivated, since their audit trail has to
        outlive their employment.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this user.
        required: true
      tags:
      - users
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
          description: ''
    put:
      operationId: users_update
      description: |-
        Colleagues.

        There is no create: accounts come from invitations, because an
        administrator who can set a password can read one, and the specification
        rules that out for both security and data-protection reasons. There is no
        delete either — a leaver is deactivated, since their audit trail has to
        outlive their employment.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this user.
        required: true
      tags:
      - users
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserUpdateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/UserUpdateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UserUpdateRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserUpdate'
          description: ''
    patch:
      operationId: users_partial_update
      description: |-
        Colleagues.

        There is no create: accounts come from invitations, because an
        administrator who can set a password can read one, and the specification
        rules that out for both security and data-protection reasons. There is no
        delete either — a leaver is deactivated, since their audit trail has to
        outlive their employment.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this user.
        required: true
      tags:
      - users
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedUserUpdateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedUserUpdateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedUserUpdateRequest'
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserUpdate'
          description: ''
  /api/v1/users/{id}/customers/:
    put:
      operationId: users_customers_update
      description: Replace the set of customers this technician may see.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this user.
        required: true
      tags:
      - users
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignedCustomersRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AssignedCustomersRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AssignedCustomersRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
          description: ''
  /api/v1/users/{id}/deactivate/:
    post:
      operationId: users_deactivate_create
      description: |-
        Colleagues.

        There is no create: accounts come from invitations, because an
        administrator who can set a password can read one, and the specification
        rules that out for both security and data-protection reasons. There is no
        delete either — a leaver is deactivated, since their audit trail has to
        outlive their employment.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        description: A UUID string identifying this user.
        required: true
      tags:
      - users
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
          description: ''
components:
  schemas:
    ActionEnum:
      enum:
      - create
      - update
      - delete
      type: string
      description: |-
        * `create` - Create
        * `update` - Update
        * `delete` - Delete
    AddElevatorsRequest:
      type: object
      properties:
        elevator_ids:
          type: array
          items:
            type: string
            format: uuid
        unit_price:
          type: string
          format: decimal
          pattern: ^-?\d{0,10}(?:\.\d{0,2})?$
          nullable: true
      required:
      - elevator_ids
    AssignedCustomersRequest:
      type: object
      properties:
        customer_ids:
          type: array
          items:
            type: string
            format: uuid
      required:
      - customer_ids
    Attachment:
      type: object
      description: |-
        What a client is told about a file.

        `storage_key` and `storage_backend` are absent on purpose. They are how the
        server finds the bytes, not facts about the document, and publishing them
        would tie the API contract to the layout of a bucket that is expected to
        change when the personal-data categories move provider.
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        object_type:
          allOf:
          - $ref: '#/components/schemas/AttachmentObjectType'
          readOnly: true
        object_id:
          type: string
          format: uuid
          readOnly: true
        category:
          allOf:
          - $ref: '#/components/schemas/AttachmentCategory'
          readOnly: true
        original_filename:
          type: string
          readOnly: true
        mime_type:
          type: string
          readOnly: true
        size_bytes:
          type: integer
          readOnly: true
        uploaded_by:
          type: string
          format: uuid
          readOnly: true
          nullable: true
        uploaded_by_name:
          type: string
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - category
      - created_at
      - id
      - mime_type
      - object_id
      - object_type
      - original_filename
      - size_bytes
      - uploaded_by
      - uploaded_by_name
    AttachmentCategory:
      enum:
      - photo
      - ce_certificate
      - declaration_of_conformity
      - permit
      - signed_contract
      - inspection_report
      - logo
      - other
      type: string
      description: |-
        * `photo` - Photo
        * `ce_certificate` - CE certificate
        * `declaration_of_conformity` - Declaration of conformity
        * `permit` - Permit
        * `signed_contract` - Signed contract
        * `inspection_report` - Inspection report
        * `logo` - Logo
        * `other` - Other
    AttachmentConfirmRequest:
      type: object
      description: |-
        The second half of an upload.

        Only the key and the display name: everything else — which record the file
        belongs to, its category, its size, its type — is read back from the key and
        from storage, so the two calls cannot contradict each other.
      properties:
        storage_key:
          type: string
          minLength: 1
          maxLength: 500
        original_filename:
          type: string
          minLength: 1
          maxLength: 255
      required:
      - original_filename
      - storage_key
    AttachmentObjectType:
      enum:
      - elevator
      - building
      - contract
      - customer
      - company
      - user
      type: string
      description: |-
        * `elevator` - Elevator
        * `building` - Building
        * `contract` - Contract
        * `customer` - Customer
        * `company` - Company
        * `user` - User
    AuditLog:
      type: object
      properties:
        company_id:
          type: string
          format: uuid
          readOnly: true
        user_id:
          type: string
          format: uuid
          readOnly: true
          nullable: true
        user_name:
          type: string
          description: |-
            Who did it, by name.

            The names are looked up once for the whole page and handed in through
            the context — see the viewset. There is no foreign key to join on, and
            that is deliberate: the trail has to survive the hard deletion of what
            it describes, so it holds a plain id.

            Empty string rather than null for a write with no actor — a background
            job, a bootstrap flow — since the client prints this directly.
          readOnly: true
        table_name:
          type: string
          readOnly: true
        record_id:
          type: string
          format: uuid
          readOnly: true
        action:
          allOf:
          - $ref: '#/components/schemas/ActionEnum'
          readOnly: true
        old_values:
          readOnly: true
          nullable: true
        new_values:
          readOnly: true
          nullable: true
        ip_address:
          type: string
          readOnly: true
          nullable: true
        created_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - action
      - company_id
      - created_at
      - ip_address
      - new_values
      - old_values
      - record_id
      - table_name
      - user_id
      - user_name
    BillingPeriodEnum:
      enum:
      - monthly
      - quarterly
      - semiannual
      - annual
      type: string
      description: |-
        * `monthly` - Monthly
        * `quarterly` - Quarterly
        * `semiannual` - Semiannual
        * `annual` - Annual
    BlankEnum:
      enum:
      - ''
    BuildingRead:
      type: object
      description: |-
        The named address behind a `neighborhood` foreign key.

        Every one of these keys is nullable — a record can be entered before anyone
        knows where it is — so the fields are declared nullable too. Without that
        the contract promises a string on rows that hold nothing, a generated client
        reads the field as always present, and the screen prints an empty line
        without anything having failed.
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        name:
          type: string
          readOnly: true
        type:
          allOf:
          - $ref: '#/components/schemas/BuildingType'
          readOnly: true
        customer_id:
          type: string
          format: uuid
          readOnly: true
        customer_name:
          type: string
          readOnly: true
        complex_id:
          type: string
          format: uuid
          nullable: true
          readOnly: true
        complex_name:
          type: string
          readOnly: true
        neighborhood_id:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
          readOnly: true
        neighborhood_name:
          type: string
          readOnly: true
          nullable: true
        district_name:
          type: string
          readOnly: true
          nullable: true
        province_name:
          type: string
          readOnly: true
          nullable: true
        street:
          type: string
          readOnly: true
        building_number:
          type: string
          readOnly: true
        address_note:
          type: string
          readOnly: true
        latitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          readOnly: true
          nullable: true
        longitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          readOnly: true
          nullable: true
        floor_count:
          type: integer
          readOnly: true
          nullable: true
        unit_count:
          type: integer
          readOnly: true
          nullable: true
        is_active:
          type: boolean
          readOnly: true
        elevator_count:
          type: integer
          readOnly: true
          default: 0
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - address_note
      - building_number
      - complex_id
      - complex_name
      - created_at
      - customer_id
      - customer_name
      - district_name
      - elevator_count
      - floor_count
      - id
      - is_active
      - latitude
      - longitude
      - name
      - neighborhood_id
      - neighborhood_name
      - province_name
      - street
      - type
      - unit_count
      - updated_at
    BuildingType:
      enum:
      - residential
      - commercial
      - mixed_use
      - public
      - hospital
      - mall
      - hotel
      - school
      - industrial
      type: string
      description: |-
        * `residential` - Residential
        * `commercial` - Commercial
        * `mixed_use` - Mixed use
        * `public` - Public
        * `hospital` - Hospital
        * `mall` - Mall
        * `hotel` - Hotel
        * `school` - School
        * `industrial` - Industrial
    BuildingWrite:
      type: object
      properties:
        customer:
          type: string
          format: uuid
        complex:
          type: string
          format: uuid
          nullable: true
        name:
          type: string
          maxLength: 150
        type:
          $ref: '#/components/schemas/BuildingType'
        neighborhood:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        street:
          type: string
          maxLength: 150
        building_number:
          type: string
          maxLength: 20
        address_note:
          type: string
        latitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
        longitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
        floor_count:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        unit_count:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        is_active:
          type: boolean
      required:
      - address_note
      - customer
      - name
      - type
    BuildingWriteRequest:
      type: object
      properties:
        customer:
          type: string
          format: uuid
        complex:
          type: string
          format: uuid
          nullable: true
        name:
          type: string
          minLength: 1
          maxLength: 150
        type:
          $ref: '#/components/schemas/BuildingType'
        neighborhood:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        street:
          type: string
          maxLength: 150
        building_number:
          type: string
          maxLength: 20
        address_note:
          type: string
          minLength: 1
        latitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
        longitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
        floor_count:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        unit_count:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        is_active:
          type: boolean
      required:
      - address_note
      - customer
      - name
      - type
    Company:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        legal_name:
          type: string
          maxLength: 200
        display_name:
          type: string
          maxLength: 80
        tax_office:
          type: string
          maxLength: 100
        tax_number:
          type: string
          maxLength: 11
        mersis_number:
          type: string
          maxLength: 16
        trade_registry_number:
          type: string
          maxLength: 30
        neighborhood:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        street:
          type: string
          maxLength: 150
        building_number:
          type: string
          maxLength: 20
        unit_number:
          type: string
          maxLength: 20
        phone:
          type: string
          maxLength: 20
        email:
          oneOf:
          - type: string
            format: email
            maxLength: 150
          - type: string
            maxLength: 0
        website:
          type: string
          maxLength: 150
        logo:
          type: string
          format: uuid
          nullable: true
        logo_url:
          type: string
          readOnly: true
        is_active:
          type: boolean
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - created_at
      - display_name
      - id
      - is_active
      - legal_name
      - logo_url
    ComplexRead:
      type: object
      description: |-
        The named address behind a `neighborhood` foreign key.

        Every one of these keys is nullable — a record can be entered before anyone
        knows where it is — so the fields are declared nullable too. Without that
        the contract promises a string on rows that hold nothing, a generated client
        reads the field as always present, and the screen prints an empty line
        without anything having failed.
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        name:
          type: string
          readOnly: true
        customer_id:
          type: string
          format: uuid
          readOnly: true
        customer_name:
          type: string
          readOnly: true
        neighborhood_id:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
          readOnly: true
        neighborhood_name:
          type: string
          readOnly: true
          nullable: true
        district_name:
          type: string
          readOnly: true
          nullable: true
        province_name:
          type: string
          readOnly: true
          nullable: true
        street:
          type: string
          readOnly: true
        building_number:
          type: string
          readOnly: true
        latitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          readOnly: true
          nullable: true
        longitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          readOnly: true
          nullable: true
        notes:
          type: string
          readOnly: true
        building_count:
          type: integer
          readOnly: true
          default: 0
        elevator_count:
          type: integer
          readOnly: true
          default: 0
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - building_count
      - building_number
      - created_at
      - customer_id
      - customer_name
      - district_name
      - elevator_count
      - id
      - latitude
      - longitude
      - name
      - neighborhood_id
      - neighborhood_name
      - notes
      - province_name
      - street
      - updated_at
    ComplexWrite:
      type: object
      properties:
        customer:
          type: string
          format: uuid
        name:
          type: string
          maxLength: 150
        neighborhood:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        street:
          type: string
          maxLength: 150
        building_number:
          type: string
          maxLength: 20
        latitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
        longitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
        notes:
          type: string
      required:
      - customer
      - name
    ComplexWriteRequest:
      type: object
      properties:
        customer:
          type: string
          format: uuid
        name:
          type: string
          minLength: 1
          maxLength: 150
        neighborhood:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        street:
          type: string
          maxLength: 150
        building_number:
          type: string
          maxLength: 20
        latitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
        longitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
        notes:
          type: string
      required:
      - customer
      - name
    ContactRole:
      enum:
      - manager
      - auditor
      - caretaker
      - technical_lead
      - accounting
      - other
      type: string
      description: |-
        * `manager` - Manager
        * `auditor` - Auditor
        * `caretaker` - Caretaker
        * `technical_lead` - Technical lead
        * `accounting` - Accounting
        * `other` - Other
    ContractLine:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        elevator_id:
          type: string
          format: uuid
          readOnly: true
        registration_number:
          type: string
          readOnly: true
        elevator_name:
          type: string
          readOnly: true
        building_name:
          type: string
          readOnly: true
        unit_price:
          type: string
          format: decimal
          pattern: ^-?\d{0,10}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
        added_at:
          type: string
          format: date
          readOnly: true
        removed_at:
          type: string
          format: date
          readOnly: true
          nullable: true
      required:
      - added_at
      - building_name
      - elevator_id
      - elevator_name
      - id
      - registration_number
      - removed_at
      - unit_price
    ContractRead:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        contract_number:
          type: string
          readOnly: true
        customer_id:
          type: string
          format: uuid
          readOnly: true
        customer_name:
          type: string
          readOnly: true
        status:
          allOf:
          - $ref: '#/components/schemas/ContractStatus'
          readOnly: true
        scope:
          allOf:
          - $ref: '#/components/schemas/ContractScope'
          readOnly: true
        start_date:
          type: string
          format: date
          readOnly: true
        end_date:
          type: string
          format: date
          readOnly: true
        pricing_type:
          allOf:
          - $ref: '#/components/schemas/PricingTypeEnum'
          readOnly: true
        monthly_fee:
          type: string
          format: decimal
          pattern: ^-?\d{0,10}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
        currency:
          type: string
          readOnly: true
        vat_rate:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
        billing_period:
          allOf:
          - $ref: '#/components/schemas/BillingPeriodEnum'
          readOnly: true
        auto_renew:
          type: boolean
          readOnly: true
        renewal_notice_days:
          type: integer
          readOnly: true
        previous_contract_id:
          type: string
          format: uuid
          nullable: true
          readOnly: true
        terminated_at:
          type: string
          format: date
          readOnly: true
          nullable: true
        termination_reason:
          type: string
          readOnly: true
        notes:
          type: string
          readOnly: true
        elevator_count:
          type: integer
          readOnly: true
          default: 0
        previous_contract_number:
          type: string
          readOnly: true
          default: ''
        monthly_subtotal:
          type: string
          format: decimal
          pattern: ^-?\d{0,12}(?:\.\d{0,2})?$
          readOnly: true
        vat_status:
          allOf:
          - $ref: '#/components/schemas/VatStatusEnum'
          readOnly: true
        vat_amount:
          type: string
          format: decimal
          pattern: ^-?\d{0,12}(?:\.\d{0,2})?$
          nullable: true
          readOnly: true
        monthly_total:
          type: string
          format: decimal
          pattern: ^-?\d{0,12}(?:\.\d{0,2})?$
          nullable: true
          readOnly: true
        lines:
          type: array
          items:
            $ref: '#/components/schemas/ContractLine'
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - auto_renew
      - billing_period
      - contract_number
      - created_at
      - currency
      - customer_id
      - customer_name
      - elevator_count
      - end_date
      - id
      - lines
      - monthly_fee
      - monthly_subtotal
      - monthly_total
      - notes
      - previous_contract_id
      - previous_contract_number
      - pricing_type
      - renewal_notice_days
      - scope
      - start_date
      - status
      - terminated_at
      - termination_reason
      - updated_at
      - vat_amount
      - vat_rate
      - vat_status
    ContractScope:
      enum:
      - maintenance_only
      - maintenance_and_repair
      - full_coverage
      type: string
      description: |-
        * `maintenance_only` - Maintenance only
        * `maintenance_and_repair` - Maintenance and repair
        * `full_coverage` - Full coverage
    ContractStatus:
      enum:
      - draft
      - active
      - expired
      - terminated
      - renewed
      type: string
      description: |-
        * `draft` - Draft
        * `active` - Active
        * `expired` - Expired
        * `terminated` - Terminated
        * `renewed` - Renewed
    ContractWrite:
      type: object
      properties:
        customer:
          type: string
          format: uuid
        contract_number:
          type: string
          maxLength: 30
        status:
          $ref: '#/components/schemas/ContractStatus'
        scope:
          $ref: '#/components/schemas/ContractScope'
        start_date:
          type: string
          format: date
        end_date:
          type: string
          format: date
        pricing_type:
          $ref: '#/components/schemas/PricingTypeEnum'
        monthly_fee:
          type: string
          format: decimal
          pattern: ^-?\d{0,10}(?:\.\d{0,2})?$
          nullable: true
        currency:
          type: string
          maxLength: 3
        vat_rate:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
        billing_period:
          $ref: '#/components/schemas/BillingPeriodEnum'
        auto_renew:
          type: boolean
        renewal_notice_days:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
        notes:
          type: string
      required:
      - customer
      - end_date
      - pricing_type
      - scope
      - start_date
      - vat_rate
    ContractWriteRequest:
      type: object
      properties:
        customer:
          type: string
          format: uuid
        contract_number:
          type: string
          minLength: 1
          maxLength: 30
        status:
          $ref: '#/components/schemas/ContractStatus'
        scope:
          $ref: '#/components/schemas/ContractScope'
        start_date:
          type: string
          format: date
        end_date:
          type: string
          format: date
        pricing_type:
          $ref: '#/components/schemas/PricingTypeEnum'
        monthly_fee:
          type: string
          format: decimal
          pattern: ^-?\d{0,10}(?:\.\d{0,2})?$
          nullable: true
        currency:
          type: string
          minLength: 1
          maxLength: 3
        vat_rate:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
        billing_period:
          $ref: '#/components/schemas/BillingPeriodEnum'
        auto_renew:
          type: boolean
        renewal_notice_days:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
        notes:
          type: string
      required:
      - customer
      - end_date
      - pricing_type
      - scope
      - start_date
      - vat_rate
    ControlTypeEnum:
      enum:
      - simple_collective
      - down_collective
      - full_collective
      - group_control
      type: string
      description: |-
        * `simple_collective` - Simple collective
        * `down_collective` - Down collective
        * `full_collective` - Full collective
        * `group_control` - Group control
    CurrentContract:
      type: object
      description: |-
        The contract covering this elevator right now, if any.

        A compact block rather than the whole contract: the detail screen shows four
        facts, and embedding a full contract here would drag its lines — every other
        elevator in the same agreement — into a response about one lift.
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        contract_number:
          type: string
          readOnly: true
        scope:
          type: string
          readOnly: true
        end_date:
          type: string
          format: date
          readOnly: true
        unit_price:
          type: string
          format: decimal
          pattern: ^-?\d{0,10}(?:\.\d{0,2})?$
          readOnly: true
      required:
      - contract_number
      - end_date
      - id
      - scope
      - unit_price
    CurrentUser:
      type: object
      description: |-
        What `/auth/me` returns.

        Read-only, and narrower than the model on purpose: `national_id`,
        `password`, `failed_login_count` and `locked_until` are all on User and none
        of them belong in a response.
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        company_id:
          type: string
          format: uuid
          readOnly: true
          nullable: true
        company_name:
          type: string
          readOnly: true
        first_name:
          type: string
          readOnly: true
        last_name:
          type: string
          readOnly: true
        full_name:
          type: string
          readOnly: true
        email:
          type: string
          format: email
          readOnly: true
        phone:
          type: string
          readOnly: true
        role:
          allOf:
          - $ref: '#/components/schemas/UserRole'
          readOnly: true
        is_active:
          type: boolean
          readOnly: true
        is_email_verified:
          type: boolean
          readOnly: true
        last_login_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
        certificate_number:
          type: string
          readOnly: true
        certificate_valid_until:
          type: string
          format: date
          readOnly: true
          nullable: true
      required:
      - certificate_number
      - certificate_valid_until
      - company_id
      - company_name
      - email
      - first_name
      - full_name
      - id
      - is_active
      - is_email_verified
      - last_login_at
      - last_name
      - phone
      - role
    CustomerContactNestedWriteRequest:
      type: object
      description: |-
        The same contact, created under `/customers/{id}/contacts`.

        `customer` is gone from the field list rather than made read-only, so
        `StrictMixin` refuses a body that names it. Two sources for one value is how
        they come to disagree, and the path is the source.
      properties:
        full_name:
          type: string
          minLength: 1
          maxLength: 120
        role:
          allOf:
          - $ref: '#/components/schemas/ContactRole'
          default: other
        phone:
          type: string
          maxLength: 20
        email:
          oneOf:
          - type: string
            format: email
            maxLength: 150
          - type: string
            maxLength: 0
        is_primary:
          type: boolean
        notes:
          type: string
      required:
      - full_name
    CustomerContactRead:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        customer_id:
          type: string
          format: uuid
          readOnly: true
        full_name:
          type: string
          readOnly: true
        role:
          allOf:
          - $ref: '#/components/schemas/ContactRole'
          readOnly: true
        phone:
          type: string
          readOnly: true
        email:
          type: string
          format: email
          readOnly: true
        is_primary:
          type: boolean
          readOnly: true
        notes:
          type: string
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - created_at
      - customer_id
      - email
      - full_name
      - id
      - is_primary
      - notes
      - phone
      - role
      - updated_at
    CustomerContactWrite:
      type: object
      description: |-
        Rejects fields the serializer does not declare.

        DRF ignores them, so a mistyped key returns 200 and the value silently
        never arrives.
      properties:
        customer:
          type: string
          format: uuid
        full_name:
          type: string
          maxLength: 120
        role:
          allOf:
          - $ref: '#/components/schemas/ContactRole'
          default: other
        phone:
          type: string
          maxLength: 20
        email:
          oneOf:
          - type: string
            format: email
            maxLength: 150
          - type: string
            maxLength: 0
        is_primary:
          type: boolean
        notes:
          type: string
      required:
      - customer
      - full_name
    CustomerContactWriteRequest:
      type: object
      description: |-
        Rejects fields the serializer does not declare.

        DRF ignores them, so a mistyped key returns 200 and the value silently
        never arrives.
      properties:
        customer:
          type: string
          format: uuid
        full_name:
          type: string
          minLength: 1
          maxLength: 120
        role:
          allOf:
          - $ref: '#/components/schemas/ContactRole'
          default: other
        phone:
          type: string
          maxLength: 20
        email:
          oneOf:
          - type: string
            format: email
            maxLength: 150
          - type: string
            maxLength: 0
        is_primary:
          type: boolean
        notes:
          type: string
      required:
      - customer
      - full_name
    CustomerRead:
      type: object
      description: |-
        The named address behind a `neighborhood` foreign key.

        Every one of these keys is nullable — a record can be entered before anyone
        knows where it is — so the fields are declared nullable too. Without that
        the contract promises a string on rows that hold nothing, a generated client
        reads the field as always present, and the screen prints an empty line
        without anything having failed.
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        type:
          allOf:
          - $ref: '#/components/schemas/CustomerType'
          readOnly: true
        legal_name:
          type: string
          readOnly: true
        tax_office:
          type: string
          readOnly: true
        tax_number:
          type: string
          readOnly: true
        phone:
          type: string
          readOnly: true
        email:
          type: string
          format: email
          readOnly: true
        neighborhood_id:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
          readOnly: true
        neighborhood_name:
          type: string
          readOnly: true
          nullable: true
        district_name:
          type: string
          readOnly: true
          nullable: true
        province_name:
          type: string
          readOnly: true
          nullable: true
        street:
          type: string
          readOnly: true
        building_number:
          type: string
          readOnly: true
        unit_number:
          type: string
          readOnly: true
        notes:
          type: string
          readOnly: true
        is_active:
          type: boolean
          readOnly: true
        contacts:
          type: array
          items:
            $ref: '#/components/schemas/CustomerContactRead'
          readOnly: true
        building_count:
          type: integer
          readOnly: true
          default: 0
        elevator_count:
          type: integer
          readOnly: true
          default: 0
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - building_count
      - building_number
      - contacts
      - created_at
      - district_name
      - elevator_count
      - email
      - id
      - is_active
      - legal_name
      - neighborhood_id
      - neighborhood_name
      - notes
      - phone
      - province_name
      - street
      - tax_number
      - tax_office
      - type
      - unit_number
      - updated_at
    CustomerType:
      enum:
      - complex_management
      - building_management
      - corporate
      - public
      - individual
      type: string
      description: |-
        * `complex_management` - Complex management
        * `building_management` - Building management
        * `corporate` - Corporate
        * `public` - Public
        * `individual` - Individual
    CustomerWrite:
      type: object
      description: |-
        Rejects fields the serializer does not declare.

        DRF ignores them, so a mistyped key returns 200 and the value silently
        never arrives.
      properties:
        type:
          $ref: '#/components/schemas/CustomerType'
        legal_name:
          type: string
          maxLength: 200
        tax_office:
          type: string
          maxLength: 100
        tax_number:
          type: string
          maxLength: 11
        phone:
          type: string
          maxLength: 20
        email:
          oneOf:
          - type: string
            format: email
            maxLength: 150
          - type: string
            maxLength: 0
        neighborhood:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        street:
          type: string
          maxLength: 150
        building_number:
          type: string
          maxLength: 20
        unit_number:
          type: string
          maxLength: 20
        notes:
          type: string
        is_active:
          type: boolean
      required:
      - legal_name
      - type
    CustomerWriteRequest:
      type: object
      description: |-
        Rejects fields the serializer does not declare.

        DRF ignores them, so a mistyped key returns 200 and the value silently
        never arrives.
      properties:
        type:
          $ref: '#/components/schemas/CustomerType'
        legal_name:
          type: string
          minLength: 1
          maxLength: 200
        tax_office:
          type: string
          maxLength: 100
        tax_number:
          type: string
          maxLength: 11
        national_id:
          type: string
          writeOnly: true
          maxLength: 255
        phone:
          type: string
          maxLength: 20
        email:
          oneOf:
          - type: string
            format: email
            maxLength: 150
          - type: string
            maxLength: 0
        neighborhood:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        street:
          type: string
          maxLength: 150
        building_number:
          type: string
          maxLength: 20
        unit_number:
          type: string
          maxLength: 20
        notes:
          type: string
        is_active:
          type: boolean
      required:
      - legal_name
      - type
    District:
      type: object
      properties:
        id:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
        province_id:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          readOnly: true
        name:
          type: string
          maxLength: 80
      required:
      - id
      - name
      - province_id
    DoorTypeEnum:
      enum:
      - automatic_center
      - automatic_side
      - semi_automatic
      - manual
      type: string
      description: |-
        * `automatic_center` - Automatic centre opening
        * `automatic_side` - Automatic side opening
        * `semi_automatic` - Semi automatic
        * `manual` - Manual
    DownloadUrl:
      type: object
      properties:
        download_url:
          type: string
          format: uri
        expires_in:
          type: integer
      required:
      - download_url
      - expires_in
    DriveTypeEnum:
      enum:
      - geared_electric
      - gearless_electric
      - hydraulic
      type: string
      description: |-
        * `geared_electric` - Geared electric
        * `gearless_electric` - Gearless electric
        * `hydraulic` - Hydraulic
    ElevatorByQr:
      type: object
      description: |-
        The six values that decide what happens next on site.

        Deliberately narrow: this is what a technician sees after scanning, on a
        phone, in a machine room. The full record is one tap away.
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        registration_number:
          type: string
          readOnly: true
        name:
          type: string
          readOnly: true
        building_name:
          type: string
          readOnly: true
        customer_name:
          type: string
          readOnly: true
        status:
          allOf:
          - $ref: '#/components/schemas/ElevatorStatus'
          readOnly: true
        inspection_label:
          allOf:
          - $ref: '#/components/schemas/InspectionLabelEnum'
          readOnly: true
        has_car_door:
          type: boolean
          readOnly: true
          nullable: true
        brand:
          type: string
          readOnly: true
        model:
          type: string
          readOnly: true
        capacity_kg:
          type: integer
          readOnly: true
          nullable: true
        capacity_persons:
          type: integer
          readOnly: true
          nullable: true
        stop_count:
          type: integer
          readOnly: true
          nullable: true
        speed_mps:
          type: string
          format: decimal
          pattern: ^-?\d{0,2}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
        last_inspection_date:
          type: string
          format: date
          readOnly: true
          nullable: true
        next_inspection_date:
          type: string
          format: date
          readOnly: true
          nullable: true
      required:
      - brand
      - building_name
      - capacity_kg
      - capacity_persons
      - customer_name
      - has_car_door
      - id
      - inspection_label
      - last_inspection_date
      - model
      - name
      - next_inspection_date
      - registration_number
      - speed_mps
      - status
      - stop_count
    ElevatorCategory:
      enum:
      - passenger
      - freight
      - passenger_freight
      - dumbwaiter
      - accessibility_platform
      - vehicle
      type: string
      description: |-
        * `passenger` - Passenger
        * `freight` - Freight
        * `passenger_freight` - Passenger and freight
        * `dumbwaiter` - Dumbwaiter
        * `accessibility_platform` - Accessibility platform
        * `vehicle` - Vehicle
    ElevatorDetail:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        building_id:
          type: string
          format: uuid
          readOnly: true
        building_name:
          type: string
          readOnly: true
        customer_id:
          type: string
          format: uuid
          readOnly: true
        customer_name:
          type: string
          readOnly: true
        complex_name:
          type: string
          readOnly: true
        current_contract:
          allOf:
          - $ref: '#/components/schemas/CurrentContract'
          nullable: true
          readOnly: true
        registration_number:
          type: string
          readOnly: true
        internal_code:
          type: string
          readOnly: true
        name:
          type: string
          readOnly: true
        qr_token:
          type: string
          readOnly: true
        qr_token_generated_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
        category:
          allOf:
          - $ref: '#/components/schemas/ElevatorCategory'
          readOnly: true
        drive_type:
          allOf:
          - $ref: '#/components/schemas/DriveTypeEnum'
          readOnly: true
        control_type:
          allOf:
          - $ref: '#/components/schemas/ControlTypeEnum'
          readOnly: true
        door_type:
          allOf:
          - $ref: '#/components/schemas/DoorTypeEnum'
          readOnly: true
        has_car_door:
          type: boolean
          readOnly: true
          nullable: true
        machine_room:
          allOf:
          - $ref: '#/components/schemas/MachineRoomEnum'
          readOnly: true
        capacity_kg:
          type: integer
          readOnly: true
          nullable: true
        capacity_persons:
          type: integer
          readOnly: true
          nullable: true
        stop_count:
          type: integer
          readOnly: true
          nullable: true
        entrance_count:
          type: integer
          readOnly: true
          nullable: true
        speed_mps:
          type: string
          format: decimal
          pattern: ^-?\d{0,2}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
        pit_depth_mm:
          type: integer
          readOnly: true
          nullable: true
        headroom_mm:
          type: integer
          readOnly: true
          nullable: true
        car_weight_kg:
          type: integer
          readOnly: true
          nullable: true
        brand:
          type: string
          readOnly: true
        model:
          type: string
          readOnly: true
        serial_number:
          type: string
          readOnly: true
        manufacturer:
          type: string
          readOnly: true
        installer:
          type: string
          readOnly: true
        installation_date:
          type: string
          format: date
          readOnly: true
          nullable: true
        commissioning_date:
          type: string
          format: date
          readOnly: true
          nullable: true
        ce_certificate_number:
          type: string
          readOnly: true
        warranty_end_date:
          type: string
          format: date
          readOnly: true
          nullable: true
        last_inspection_date:
          type: string
          format: date
          readOnly: true
          nullable: true
        inspection_label:
          allOf:
          - $ref: '#/components/schemas/InspectionLabelEnum'
          readOnly: true
        next_inspection_date:
          type: string
          format: date
          readOnly: true
          nullable: true
        inspection_body:
          type: string
          readOnly: true
        inspection_report_number:
          type: string
          readOnly: true
        status:
          allOf:
          - $ref: '#/components/schemas/ElevatorStatus'
          readOnly: true
        maintenance_interval_days:
          type: integer
          readOnly: true
        notes:
          type: string
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - brand
      - building_id
      - building_name
      - capacity_kg
      - capacity_persons
      - car_weight_kg
      - category
      - ce_certificate_number
      - commissioning_date
      - complex_name
      - control_type
      - created_at
      - current_contract
      - customer_id
      - customer_name
      - door_type
      - drive_type
      - entrance_count
      - has_car_door
      - headroom_mm
      - id
      - inspection_body
      - inspection_label
      - inspection_report_number
      - installation_date
      - installer
      - internal_code
      - last_inspection_date
      - machine_room
      - maintenance_interval_days
      - manufacturer
      - model
      - name
      - next_inspection_date
      - notes
      - pit_depth_mm
      - qr_token
      - qr_token_generated_at
      - registration_number
      - serial_number
      - speed_mps
      - status
      - stop_count
      - updated_at
      - warranty_end_date
    ElevatorList:
      type: object
      description: |-
        What the list screen needs — seven columns' worth, not all 31 fields.

        The rest are record fields rather than search criteria and live on the
        detail response; sending them all would make the heaviest screen in the
        product heavier for nothing.
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        registration_number:
          type: string
          readOnly: true
        name:
          type: string
          readOnly: true
        category:
          allOf:
          - $ref: '#/components/schemas/ElevatorCategory'
          readOnly: true
        stop_count:
          type: integer
          readOnly: true
          nullable: true
        has_car_door:
          type: boolean
          readOnly: true
          nullable: true
        building_id:
          type: string
          format: uuid
          readOnly: true
        building_name:
          type: string
          readOnly: true
        customer_name:
          type: string
          readOnly: true
        status:
          allOf:
          - $ref: '#/components/schemas/ElevatorStatus'
          readOnly: true
        inspection_label:
          allOf:
          - $ref: '#/components/schemas/InspectionLabelEnum'
          readOnly: true
        next_inspection_date:
          type: string
          format: date
          readOnly: true
          nullable: true
        brand:
          type: string
          readOnly: true
        model:
          type: string
          readOnly: true
      required:
      - brand
      - building_id
      - building_name
      - category
      - customer_name
      - has_car_door
      - id
      - inspection_label
      - model
      - name
      - next_inspection_date
      - registration_number
      - status
      - stop_count
    ElevatorStatus:
      enum:
      - active
      - suspended
      - sealed
      - out_of_service
      - uncontracted
      type: string
      description: |-
        * `active` - Active
        * `suspended` - Suspended
        * `sealed` - Sealed
        * `out_of_service` - Out of service
        * `uncontracted` - Uncontracted
    ElevatorWrite:
      type: object
      properties:
        building:
          type: string
          format: uuid
        registration_number:
          type: string
          maxLength: 30
        internal_code:
          type: string
          maxLength: 30
        name:
          type: string
          maxLength: 100
        category:
          oneOf:
          - $ref: '#/components/schemas/ElevatorCategory'
          - $ref: '#/components/schemas/BlankEnum'
        drive_type:
          oneOf:
          - $ref: '#/components/schemas/DriveTypeEnum'
          - $ref: '#/components/schemas/BlankEnum'
        control_type:
          oneOf:
          - $ref: '#/components/schemas/ControlTypeEnum'
          - $ref: '#/components/schemas/BlankEnum'
        door_type:
          oneOf:
          - $ref: '#/components/schemas/DoorTypeEnum'
          - $ref: '#/components/schemas/BlankEnum'
        has_car_door:
          type: boolean
          nullable: true
        machine_room:
          oneOf:
          - $ref: '#/components/schemas/MachineRoomEnum'
          - $ref: '#/components/schemas/BlankEnum'
        capacity_kg:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        capacity_persons:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        stop_count:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        entrance_count:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        speed_mps:
          type: string
          format: decimal
          pattern: ^-?\d{0,2}(?:\.\d{0,2})?$
          nullable: true
        pit_depth_mm:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        headroom_mm:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        car_weight_kg:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        brand:
          type: string
          maxLength: 100
        model:
          type: string
          maxLength: 100
        serial_number:
          type: string
          maxLength: 100
        manufacturer:
          type: string
          maxLength: 150
        installer:
          type: string
          maxLength: 150
        installation_date:
          type: string
          format: date
          nullable: true
        commissioning_date:
          type: string
          format: date
          nullable: true
        ce_certificate_number:
          type: string
          maxLength: 60
        warranty_end_date:
          type: string
          format: date
          nullable: true
        last_inspection_date:
          type: string
          format: date
          nullable: true
        inspection_label:
          allOf:
          - $ref: '#/components/schemas/InspectionLabelEnum'
          default: none
        next_inspection_date:
          type: string
          format: date
          nullable: true
        inspection_body:
          type: string
          maxLength: 150
        inspection_report_number:
          type: string
          maxLength: 60
        status:
          $ref: '#/components/schemas/ElevatorWriteStatusEnum'
        maintenance_interval_days:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
        notes:
          type: string
      required:
      - building
    ElevatorWriteRequest:
      type: object
      properties:
        building:
          type: string
          format: uuid
        registration_number:
          type: string
          maxLength: 30
        internal_code:
          type: string
          maxLength: 30
        name:
          type: string
          maxLength: 100
        category:
          oneOf:
          - $ref: '#/components/schemas/ElevatorCategory'
          - $ref: '#/components/schemas/BlankEnum'
        drive_type:
          oneOf:
          - $ref: '#/components/schemas/DriveTypeEnum'
          - $ref: '#/components/schemas/BlankEnum'
        control_type:
          oneOf:
          - $ref: '#/components/schemas/ControlTypeEnum'
          - $ref: '#/components/schemas/BlankEnum'
        door_type:
          oneOf:
          - $ref: '#/components/schemas/DoorTypeEnum'
          - $ref: '#/components/schemas/BlankEnum'
        has_car_door:
          type: boolean
          nullable: true
        machine_room:
          oneOf:
          - $ref: '#/components/schemas/MachineRoomEnum'
          - $ref: '#/components/schemas/BlankEnum'
        capacity_kg:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        capacity_persons:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        stop_count:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        entrance_count:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        speed_mps:
          type: string
          format: decimal
          pattern: ^-?\d{0,2}(?:\.\d{0,2})?$
          nullable: true
        pit_depth_mm:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        headroom_mm:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        car_weight_kg:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        brand:
          type: string
          maxLength: 100
        model:
          type: string
          maxLength: 100
        serial_number:
          type: string
          maxLength: 100
        manufacturer:
          type: string
          maxLength: 150
        installer:
          type: string
          maxLength: 150
        installation_date:
          type: string
          format: date
          nullable: true
        commissioning_date:
          type: string
          format: date
          nullable: true
        ce_certificate_number:
          type: string
          maxLength: 60
        warranty_end_date:
          type: string
          format: date
          nullable: true
        last_inspection_date:
          type: string
          format: date
          nullable: true
        inspection_label:
          allOf:
          - $ref: '#/components/schemas/InspectionLabelEnum'
          default: none
        next_inspection_date:
          type: string
          format: date
          nullable: true
        inspection_body:
          type: string
          maxLength: 150
        inspection_report_number:
          type: string
          maxLength: 60
        status:
          $ref: '#/components/schemas/ElevatorWriteStatusEnum'
        maintenance_interval_days:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
        notes:
          type: string
      required:
      - building
    ElevatorWriteStatusEnum:
      enum:
      - active
      - suspended
      - sealed
      - out_of_service
      type: string
      description: |-
        * `active` - active
        * `suspended` - suspended
        * `sealed` - sealed
        * `out_of_service` - out_of_service
    EmailVerifyRequest:
      type: object
      description: |-
        Rejects fields it does not know about.

        DRF ignores unknown keys by default, which silently swallows a typo: the
        client sends `emial`, gets a 200, and the value never arrives. Failing is
        the kinder answer.
      properties:
        token:
          type: string
          minLength: 1
          maxLength: 128
      required:
      - token
    GeocodeMatch:
      type: object
      description: One level of the address, resolved to a row the client can select.
      properties:
        id:
          type: integer
          readOnly: true
          description: Feeds straight back into the province/district/neighborhood
            endpoints.
        name:
          type: string
          readOnly: true
          description: The name as this API stores it.
        confidence:
          type: number
          format: double
          readOnly: true
          description: Trigram similarity between the provider's name and ours, 0-1.
            1.0 means the two agree once folded; anything at or above the server's
            threshold is a match, and anything below is reported as no match rather
            than as a low score.
      required:
      - confidence
      - id
      - name
    InspectionLabelEnum:
      enum:
      - green
      - blue
      - yellow
      - red
      - none
      type: string
      description: |-
        * `green` - Green
        * `blue` - Blue
        * `yellow` - Yellow
        * `red` - Red
        * `none` - None
    Invitation:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        email:
          type: string
          format: email
          readOnly: true
        first_name:
          type: string
          readOnly: true
        last_name:
          type: string
          readOnly: true
        role:
          allOf:
          - $ref: '#/components/schemas/UserRole'
          readOnly: true
        expires_at:
          type: string
          format: date-time
          readOnly: true
        accepted_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
        is_expired:
          type: boolean
          readOnly: true
        invited_by:
          type: string
          format: uuid
          readOnly: true
          nullable: true
        invited_by_name:
          type: string
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - accepted_at
      - created_at
      - email
      - expires_at
      - first_name
      - id
      - invited_by
      - invited_by_name
      - is_expired
      - last_name
      - role
    InvitationAcceptRequest:
      type: object
      description: |-
        Rejects fields it does not know about.

        DRF ignores unknown keys by default, which silently swallows a typo: the
        client sends `emial`, gets a 200, and the value never arrives. Failing is
        the kinder answer.
      properties:
        token:
          type: string
          minLength: 1
          maxLength: 128
        password:
          type: string
          writeOnly: true
          minLength: 6
      required:
      - password
      - token
    InvitationCreateRequest:
      type: object
      properties:
        email:
          type: string
          format: email
          minLength: 1
          maxLength: 150
        first_name:
          type: string
          minLength: 1
          maxLength: 60
        last_name:
          type: string
          minLength: 1
          maxLength: 60
        role:
          $ref: '#/components/schemas/UserRole'
      required:
      - email
      - first_name
      - last_name
      - role
    InvitationPreview:
      type: object
      description: |-
        What the sign-up screen may show before anyone has authenticated.

        Only what is already in the e-mail the invitee is holding: who invited them
        and under what name. Anything more would make a leaked link a way to read
        the company's data without accepting.
      properties:
        email:
          type: string
          format: email
        first_name:
          type: string
        last_name:
          type: string
        role:
          type: string
        company_name:
          type: string
        expires_at:
          type: string
          format: date-time
      required:
      - company_name
      - email
      - expires_at
      - first_name
      - last_name
      - role
    LabelRequestRequest:
      type: object
      description: |-
        Which elevators to print, in the order they should appear.

        A list rather than a filter: the user ticks rows on a screen, and a filter
        re-evaluated on the server could quietly print a different set from the one
        they were looking at.
      properties:
        elevator_ids:
          type: array
          items:
            type: string
            format: uuid
          maxItems: 240
      required:
      - elevator_ids
    LoginRequest:
      type: object
      description: |-
        Rejects fields it does not know about.

        DRF ignores unknown keys by default, which silently swallows a typo: the
        client sends `emial`, gets a 200, and the value never arrives. Failing is
        the kinder answer.
      properties:
        email:
          type: string
          format: email
          minLength: 1
          maxLength: 150
        password:
          type: string
          writeOnly: true
          minLength: 1
      required:
      - email
      - password
    MachineRoomEnum:
      enum:
      - present
      - absent
      - partial
      type: string
      description: |-
        * `present` - Present
        * `absent` - Absent
        * `partial` - Partial
    MimeTypeEnum:
      enum:
      - application/pdf
      - image/jpeg
      - image/png
      - image/webp
      type: string
      description: |-
        * `application/pdf` - application/pdf
        * `image/jpeg` - image/jpeg
        * `image/png` - image/png
        * `image/webp` - image/webp
    Neighborhood:
      type: object
      properties:
        id:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
        district_id:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          readOnly: true
        district_name:
          type: string
          readOnly: true
        province_name:
          type: string
          readOnly: true
        name:
          type: string
          maxLength: 120
        postal_code:
          type: string
          maxLength: 5
        type:
          $ref: '#/components/schemas/NeighborhoodType'
      required:
      - district_id
      - district_name
      - id
      - name
      - province_name
    NeighborhoodType:
      enum:
      - neighborhood
      - village
      - town
      type: string
      description: |-
        * `neighborhood` - Neighborhood
        * `village` - Village
        * `town` - Town
    PaginatedAttachmentList:
      type: object
      required:
      - results
      - pagination
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/Attachment'
        pagination:
          type: object
          required:
          - page
          - page_size
          - total
          - total_pages
          properties:
            page:
              type: integer
              example: 1
            page_size:
              type: integer
              example: 25
            total:
              type: integer
              example: 342
            total_pages:
              type: integer
              example: 14
    PaginatedAuditLogList:
      type: object
      required:
      - results
      - pagination
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/AuditLog'
        pagination:
          type: object
          required:
          - page
          - page_size
          - total
          - total_pages
          properties:
            page:
              type: integer
              example: 1
            page_size:
              type: integer
              example: 25
            total:
              type: integer
              example: 342
            total_pages:
              type: integer
              example: 14
    PaginatedBuildingReadList:
      type: object
      required:
      - results
      - pagination
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/BuildingRead'
        pagination:
          type: object
          required:
          - page
          - page_size
          - total
          - total_pages
          properties:
            page:
              type: integer
              example: 1
            page_size:
              type: integer
              example: 25
            total:
              type: integer
              example: 342
            total_pages:
              type: integer
              example: 14
    PaginatedComplexReadList:
      type: object
      required:
      - results
      - pagination
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/ComplexRead'
        pagination:
          type: object
          required:
          - page
          - page_size
          - total
          - total_pages
          properties:
            page:
              type: integer
              example: 1
            page_size:
              type: integer
              example: 25
            total:
              type: integer
              example: 342
            total_pages:
              type: integer
              example: 14
    PaginatedContractReadList:
      type: object
      required:
      - results
      - pagination
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/ContractRead'
        pagination:
          type: object
          required:
          - page
          - page_size
          - total
          - total_pages
          properties:
            page:
              type: integer
              example: 1
            page_size:
              type: integer
              example: 25
            total:
              type: integer
              example: 342
            total_pages:
              type: integer
              example: 14
    PaginatedCustomerContactReadList:
      type: object
      required:
      - results
      - pagination
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/CustomerContactRead'
        pagination:
          type: object
          required:
          - page
          - page_size
          - total
          - total_pages
          properties:
            page:
              type: integer
              example: 1
            page_size:
              type: integer
              example: 25
            total:
              type: integer
              example: 342
            total_pages:
              type: integer
              example: 14
    PaginatedCustomerReadList:
      type: object
      required:
      - results
      - pagination
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/CustomerRead'
        pagination:
          type: object
          required:
          - page
          - page_size
          - total
          - total_pages
          properties:
            page:
              type: integer
              example: 1
            page_size:
              type: integer
              example: 25
            total:
              type: integer
              example: 342
            total_pages:
              type: integer
              example: 14
    PaginatedElevatorListList:
      type: object
      required:
      - results
      - pagination
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/ElevatorList'
        pagination:
          type: object
          required:
          - page
          - page_size
          - total
          - total_pages
          properties:
            page:
              type: integer
              example: 1
            page_size:
              type: integer
              example: 25
            total:
              type: integer
              example: 342
            total_pages:
              type: integer
              example: 14
    PaginatedInvitationList:
      type: object
      required:
      - results
      - pagination
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/Invitation'
        pagination:
          type: object
          required:
          - page
          - page_size
          - total
          - total_pages
          properties:
            page:
              type: integer
              example: 1
            page_size:
              type: integer
              example: 25
            total:
              type: integer
              example: 342
            total_pages:
              type: integer
              example: 14
    PaginatedUserList:
      type: object
      required:
      - results
      - pagination
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/User'
        pagination:
          type: object
          required:
          - page
          - page_size
          - total
          - total_pages
          properties:
            page:
              type: integer
              example: 1
            page_size:
              type: integer
              example: 25
            total:
              type: integer
              example: 342
            total_pages:
              type: integer
              example: 14
    PasswordChangeRequest:
      type: object
      description: |-
        The current password and the wanted one.

        The new one runs through `PasswordField`, which is the same field
        registration uses — length from `settings.MIN_PASSWORD_LENGTH`, the common
        password blocklist, and the similarity check. There is no second policy
        here and there must never be one: a floor written twice is a floor that
        only moves in one of the two places.
      properties:
        current_password:
          type: string
          writeOnly: true
          minLength: 1
        new_password:
          type: string
          writeOnly: true
          minLength: 6
      required:
      - current_password
      - new_password
    PasswordResetConfirmRequest:
      type: object
      description: |-
        Rejects fields it does not know about.

        DRF ignores unknown keys by default, which silently swallows a typo: the
        client sends `emial`, gets a 200, and the value never arrives. Failing is
        the kinder answer.
      properties:
        token:
          type: string
          minLength: 1
          maxLength: 128
        password:
          type: string
          writeOnly: true
          minLength: 6
      required:
      - password
      - token
    PasswordResetRequestRequest:
      type: object
      description: |-
        Rejects fields it does not know about.

        DRF ignores unknown keys by default, which silently swallows a typo: the
        client sends `emial`, gets a 200, and the value never arrives. Failing is
        the kinder answer.
      properties:
        email:
          type: string
          format: email
          minLength: 1
          maxLength: 150
      required:
      - email
    PatchedBuildingWriteRequest:
      type: object
      properties:
        customer:
          type: string
          format: uuid
        complex:
          type: string
          format: uuid
          nullable: true
        name:
          type: string
          minLength: 1
          maxLength: 150
        type:
          $ref: '#/components/schemas/BuildingType'
        neighborhood:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        street:
          type: string
          maxLength: 150
        building_number:
          type: string
          maxLength: 20
        address_note:
          type: string
          minLength: 1
        latitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
        longitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
        floor_count:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        unit_count:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        is_active:
          type: boolean
    PatchedCompanyRequest:
      type: object
      properties:
        legal_name:
          type: string
          minLength: 1
          maxLength: 200
        display_name:
          type: string
          minLength: 1
          maxLength: 80
        tax_office:
          type: string
          maxLength: 100
        tax_number:
          type: string
          maxLength: 11
        mersis_number:
          type: string
          maxLength: 16
        trade_registry_number:
          type: string
          maxLength: 30
        neighborhood:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        street:
          type: string
          maxLength: 150
        building_number:
          type: string
          maxLength: 20
        unit_number:
          type: string
          maxLength: 20
        phone:
          type: string
          maxLength: 20
        email:
          oneOf:
          - type: string
            format: email
            maxLength: 150
          - type: string
            maxLength: 0
        website:
          type: string
          maxLength: 150
        logo:
          type: string
          format: uuid
          nullable: true
    PatchedComplexWriteRequest:
      type: object
      properties:
        customer:
          type: string
          format: uuid
        name:
          type: string
          minLength: 1
          maxLength: 150
        neighborhood:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        street:
          type: string
          maxLength: 150
        building_number:
          type: string
          maxLength: 20
        latitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
        longitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
        notes:
          type: string
    PatchedContractWriteRequest:
      type: object
      properties:
        customer:
          type: string
          format: uuid
        contract_number:
          type: string
          minLength: 1
          maxLength: 30
        status:
          $ref: '#/components/schemas/ContractStatus'
        scope:
          $ref: '#/components/schemas/ContractScope'
        start_date:
          type: string
          format: date
        end_date:
          type: string
          format: date
        pricing_type:
          $ref: '#/components/schemas/PricingTypeEnum'
        monthly_fee:
          type: string
          format: decimal
          pattern: ^-?\d{0,10}(?:\.\d{0,2})?$
          nullable: true
        currency:
          type: string
          minLength: 1
          maxLength: 3
        vat_rate:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
        billing_period:
          $ref: '#/components/schemas/BillingPeriodEnum'
        auto_renew:
          type: boolean
        renewal_notice_days:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
        notes:
          type: string
    PatchedCustomerContactWriteRequest:
      type: object
      description: |-
        Rejects fields the serializer does not declare.

        DRF ignores them, so a mistyped key returns 200 and the value silently
        never arrives.
      properties:
        customer:
          type: string
          format: uuid
        full_name:
          type: string
          minLength: 1
          maxLength: 120
        role:
          allOf:
          - $ref: '#/components/schemas/ContactRole'
          default: other
        phone:
          type: string
          maxLength: 20
        email:
          oneOf:
          - type: string
            format: email
            maxLength: 150
          - type: string
            maxLength: 0
        is_primary:
          type: boolean
        notes:
          type: string
    PatchedCustomerWriteRequest:
      type: object
      description: |-
        Rejects fields the serializer does not declare.

        DRF ignores them, so a mistyped key returns 200 and the value silently
        never arrives.
      properties:
        type:
          $ref: '#/components/schemas/CustomerType'
        legal_name:
          type: string
          minLength: 1
          maxLength: 200
        tax_office:
          type: string
          maxLength: 100
        tax_number:
          type: string
          maxLength: 11
        national_id:
          type: string
          writeOnly: true
          maxLength: 255
        phone:
          type: string
          maxLength: 20
        email:
          oneOf:
          - type: string
            format: email
            maxLength: 150
          - type: string
            maxLength: 0
        neighborhood:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        street:
          type: string
          maxLength: 150
        building_number:
          type: string
          maxLength: 20
        unit_number:
          type: string
          maxLength: 20
        notes:
          type: string
        is_active:
          type: boolean
    PatchedElevatorWriteRequest:
      type: object
      properties:
        building:
          type: string
          format: uuid
        registration_number:
          type: string
          maxLength: 30
        internal_code:
          type: string
          maxLength: 30
        name:
          type: string
          maxLength: 100
        category:
          oneOf:
          - $ref: '#/components/schemas/ElevatorCategory'
          - $ref: '#/components/schemas/BlankEnum'
        drive_type:
          oneOf:
          - $ref: '#/components/schemas/DriveTypeEnum'
          - $ref: '#/components/schemas/BlankEnum'
        control_type:
          oneOf:
          - $ref: '#/components/schemas/ControlTypeEnum'
          - $ref: '#/components/schemas/BlankEnum'
        door_type:
          oneOf:
          - $ref: '#/components/schemas/DoorTypeEnum'
          - $ref: '#/components/schemas/BlankEnum'
        has_car_door:
          type: boolean
          nullable: true
        machine_room:
          oneOf:
          - $ref: '#/components/schemas/MachineRoomEnum'
          - $ref: '#/components/schemas/BlankEnum'
        capacity_kg:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        capacity_persons:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        stop_count:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        entrance_count:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        speed_mps:
          type: string
          format: decimal
          pattern: ^-?\d{0,2}(?:\.\d{0,2})?$
          nullable: true
        pit_depth_mm:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        headroom_mm:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        car_weight_kg:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          nullable: true
        brand:
          type: string
          maxLength: 100
        model:
          type: string
          maxLength: 100
        serial_number:
          type: string
          maxLength: 100
        manufacturer:
          type: string
          maxLength: 150
        installer:
          type: string
          maxLength: 150
        installation_date:
          type: string
          format: date
          nullable: true
        commissioning_date:
          type: string
          format: date
          nullable: true
        ce_certificate_number:
          type: string
          maxLength: 60
        warranty_end_date:
          type: string
          format: date
          nullable: true
        last_inspection_date:
          type: string
          format: date
          nullable: true
        inspection_label:
          allOf:
          - $ref: '#/components/schemas/InspectionLabelEnum'
          default: none
        next_inspection_date:
          type: string
          format: date
          nullable: true
        inspection_body:
          type: string
          maxLength: 150
        inspection_report_number:
          type: string
          maxLength: 60
        status:
          $ref: '#/components/schemas/ElevatorWriteStatusEnum'
        maintenance_interval_days:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
        notes:
          type: string
    PatchedUserUpdateRequest:
      type: object
      properties:
        first_name:
          type: string
          minLength: 1
          maxLength: 60
        last_name:
          type: string
          minLength: 1
          maxLength: 60
        phone:
          type: string
          maxLength: 20
        role:
          $ref: '#/components/schemas/UserRole'
        certificate_number:
          type: string
          maxLength: 50
        certificate_valid_until:
          type: string
          format: date
          nullable: true
    PricingTypeEnum:
      enum:
      - per_elevator
      - flat
      type: string
      description: |-
        * `per_elevator` - Per elevator
        * `flat` - Flat
    Province:
      type: object
      properties:
        id:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
        name:
          type: string
          maxLength: 50
      required:
      - id
      - name
    RegisterRequest:
      type: object
      description: |-
        Rejects fields it does not know about.

        DRF ignores unknown keys by default, which silently swallows a typo: the
        client sends `emial`, gets a 200, and the value never arrives. Failing is
        the kinder answer.
      properties:
        legal_name:
          type: string
          minLength: 1
          maxLength: 200
        display_name:
          type: string
          minLength: 1
          maxLength: 80
        first_name:
          type: string
          minLength: 1
          maxLength: 60
        last_name:
          type: string
          minLength: 1
          maxLength: 60
        email:
          type: string
          format: email
          minLength: 1
          maxLength: 150
        password:
          type: string
          writeOnly: true
          minLength: 6
      required:
      - display_name
      - email
      - first_name
      - last_name
      - legal_name
      - password
    RenewRequest:
      type: object
      properties:
        start_date:
          type: string
          format: date
        end_date:
          type: string
          format: date
        carry_elevators:
          type: boolean
          default: true
        copy_terms:
          type: boolean
          default: true
      required:
      - end_date
      - start_date
    ReverseGeocode:
      type: object
      description: |-
        What a dropped pin resolves to.

        Ids rather than names, because the form the client is filling in is a
        cascade of selects over the address tables — a name would only send it back
        to look the row up, and would leave it guessing when the lookup found two.
      properties:
        province:
          allOf:
          - $ref: '#/components/schemas/GeocodeMatch'
          readOnly: true
          nullable: true
        district:
          allOf:
          - $ref: '#/components/schemas/GeocodeMatch'
          readOnly: true
          nullable: true
        neighborhood:
          allOf:
          - $ref: '#/components/schemas/GeocodeMatch'
          readOnly: true
          nullable: true
        unmatched:
          type: array
          items:
            $ref: '#/components/schemas/UnmatchedEnum'
          readOnly: true
          description: 'Levels that could not be resolved and must be chosen by the
            user. Stated explicitly rather than left as three nulls: a client that
            reads null as ''not filled in yet'' behaves differently from one that
            reads it as ''we looked and found nothing'', and only one of those asks
            the user.'
      required:
      - district
      - neighborhood
      - province
      - unmatched
    Session:
      type: object
      description: |-
        One signed-in device, not one refresh token.

        `id` is the chain rather than the row's primary key. The row is replaced
        every time the access token is refreshed, so a client that listed sessions,
        let the user read the screen, and then revoked a row id would hit a session
        that no longer exists roughly as often as not. The chain id is stable for
        as long as the session is.
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        signed_in_at:
          type: string
          format: date-time
          readOnly: true
        last_used_at:
          type: string
          format: date-time
          readOnly: true
        expires_at:
          type: string
          format: date-time
          readOnly: true
        user_agent:
          type: string
          readOnly: true
        ip_address:
          type: string
          readOnly: true
          nullable: true
        is_current:
          type: boolean
          description: |-
            Whether this is the session the request itself arrived on.

            Answered from the refresh cookie, which is the only thing that names a
            session — the access token says who the caller is, never which of their
            devices is asking.
          readOnly: true
      required:
      - expires_at
      - id
      - ip_address
      - is_current
      - last_used_at
      - signed_in_at
      - user_agent
    TerminateRequest:
      type: object
      properties:
        terminated_at:
          type: string
          format: date
        reason:
          type: string
          maxLength: 2000
      required:
      - reason
      - terminated_at
    TokenResponse:
      type: object
      description: |-
        The body of a successful sign-in.

        Only the access token is here. The refresh token goes out as an httpOnly
        cookie and never touches JavaScript — putting it in the body would make it
        readable by any script that gets injected into the page.
      properties:
        access:
          type: string
          readOnly: true
        user:
          allOf:
          - $ref: '#/components/schemas/CurrentUser'
          readOnly: true
      required:
      - access
      - user
    UnmatchedEnum:
      enum:
      - province
      - district
      - neighborhood
      type: string
      description: |-
        * `province` - province
        * `district` - district
        * `neighborhood` - neighborhood
    UploadUrlRequestRequest:
      type: object
      properties:
        object_type:
          $ref: '#/components/schemas/AttachmentObjectType'
        object_id:
          type: string
          format: uuid
        category:
          $ref: '#/components/schemas/AttachmentCategory'
        mime_type:
          $ref: '#/components/schemas/MimeTypeEnum'
        size_bytes:
          type: integer
          maximum: 10485760
          minimum: 1
      required:
      - category
      - mime_type
      - object_id
      - object_type
      - size_bytes
    UploadUrlResponse:
      type: object
      properties:
        upload_url:
          type: string
          format: uri
        storage_key:
          type: string
        expires_in:
          type: integer
        content_type:
          type: string
      required:
      - content_type
      - expires_in
      - storage_key
      - upload_url
    User:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        first_name:
          type: string
          readOnly: true
        last_name:
          type: string
          readOnly: true
        full_name:
          type: string
          readOnly: true
        email:
          type: string
          format: email
          readOnly: true
        phone:
          type: string
          readOnly: true
        role:
          allOf:
          - $ref: '#/components/schemas/UserRole'
          readOnly: true
        is_active:
          type: boolean
          readOnly: true
        is_email_verified:
          type: boolean
          readOnly: true
        certificate_number:
          type: string
          readOnly: true
        certificate_valid_until:
          type: string
          format: date
          readOnly: true
          nullable: true
        last_login_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
        assigned_customer_ids:
          type: array
          items:
            type: string
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - assigned_customer_ids
      - certificate_number
      - certificate_valid_until
      - created_at
      - email
      - first_name
      - full_name
      - id
      - is_active
      - is_email_verified
      - last_login_at
      - last_name
      - phone
      - role
    UserRole:
      enum:
      - owner
      - admin
      - operations
      - technician
      - accountant
      type: string
      description: |-
        * `owner` - Owner
        * `admin` - Admin
        * `operations` - Operations
        * `technician` - Technician
        * `accountant` - Accountant
    UserUpdate:
      type: object
      properties:
        first_name:
          type: string
          maxLength: 60
        last_name:
          type: string
          maxLength: 60
        phone:
          type: string
          maxLength: 20
        role:
          $ref: '#/components/schemas/UserRole'
        certificate_number:
          type: string
          maxLength: 50
        certificate_valid_until:
          type: string
          format: date
          nullable: true
      required:
      - first_name
      - last_name
    UserUpdateRequest:
      type: object
      properties:
        first_name:
          type: string
          minLength: 1
          maxLength: 60
        last_name:
          type: string
          minLength: 1
          maxLength: 60
        phone:
          type: string
          maxLength: 20
        role:
          $ref: '#/components/schemas/UserRole'
        certificate_number:
          type: string
          maxLength: 50
        certificate_valid_until:
          type: string
          format: date
          nullable: true
      required:
      - first_name
      - last_name
    VatStatusEnum:
      enum:
      - applied
      - zero_rated
      - unset
      type: string
      description: |-
        * `applied` - A rate is stated and charged
        * `zero_rated` - The rate is stated and it is zero
        * `unset` - No rate has been stated, so the VAT cannot be computed
    ErrorCode:
      type: string
      enum:
      - VALIDATION_ERROR
      - NOT_FOUND
      - PERMISSION_DENIED
      - AUTHENTICATION_FAILED
      - INTERNAL_ERROR
      - THROTTLED
      - SERVICE_UNAVAILABLE
      - CONSTRAINT_VIOLATION
      - API_VERSION_SUNSET
      - INVALID_TAX_NUMBER
      - INVALID_NATIONAL_ID
      - INVALID_PHONE
      - INVALID_POSTAL_CODE
      - END_DATE_BEFORE_START_DATE
      - INSTALLATION_DATE_IN_FUTURE
      - RECORD_IN_USE
      - DUPLICATE_REGISTRATION_NUMBER
      - DUPLICATE_CONTRACT_NUMBER
      - DUPLICATE_TAX_NUMBER
      - DUPLICATE_NATIONAL_ID
      - EMAIL_ALREADY_REGISTERED
      - INVITATION_ALREADY_PENDING
      - IDEMPOTENCY_KEY_REUSED
      - ELEVATOR_ALREADY_CONTRACTED
      - LAST_OWNER_CANNOT_BE_DEACTIVATED
      - ONLY_TECHNICIANS_ARE_ASSIGNED
      - CANNOT_DEACTIVATE_SELF
      - BUILDING_CUSTOMER_MISMATCH
      - TERMINATION_REASON_REQUIRED
      - FIELD_REQUIRED_FOR_CUSTOMER_TYPE
      - FIELD_NOT_VALID_FOR_CUSTOMER_TYPE
      - STATUS_NOT_USER_SELECTABLE
      - INVALID_CREDENTIALS
      - ACCOUNT_LOCKED
      - ACCOUNT_INACTIVE
      - TOKEN_INVALID
      - TOKEN_EXPIRED
      - EMAIL_NOT_VERIFIED
      - FILE_TOO_LARGE
      - UNSUPPORTED_MIME_TYPE
      - UPLOAD_NOT_COMPLETED
      - ATTACHMENT_TARGET_MISMATCH
      description: |-
        * `VALIDATION_ERROR` - Request failed validation
        * `NOT_FOUND` - Record not found
        * `PERMISSION_DENIED` - Insufficient permissions
        * `AUTHENTICATION_FAILED` - Authentication failed
        * `INTERNAL_ERROR` - Unexpected server error
        * `THROTTLED` - Rate limit exceeded
        * `SERVICE_UNAVAILABLE` - A required service is not available
        * `CONSTRAINT_VIOLATION` - The request violates a database constraint
        * `API_VERSION_SUNSET` - This API version is no longer served
        * `INVALID_TAX_NUMBER` - Tax number failed its check digit
        * `INVALID_NATIONAL_ID` - National ID failed its check digit
        * `INVALID_PHONE` - Phone number is not a valid Turkish number
        * `INVALID_POSTAL_CODE` - Postal code must be five digits
        * `END_DATE_BEFORE_START_DATE` - End date must be after the start date
        * `INSTALLATION_DATE_IN_FUTURE` - Installation date cannot be in the future
        * `RECORD_IN_USE` - Record is referenced by other records
        * `DUPLICATE_REGISTRATION_NUMBER` - Registration number already exists for this company
        * `DUPLICATE_CONTRACT_NUMBER` - Contract number already exists for this company
        * `DUPLICATE_TAX_NUMBER` - Tax number already belongs to another customer of this company
        * `DUPLICATE_NATIONAL_ID` - National ID already belongs to another customer of this company
        * `EMAIL_ALREADY_REGISTERED` - E-mail address is already registered
        * `INVITATION_ALREADY_PENDING` - This address already holds an invitation that has not been accepted
        * `IDEMPOTENCY_KEY_REUSED` - Idempotency key was reused with a different request body
        * `ELEVATOR_ALREADY_CONTRACTED` - Elevator is already covered by an open contract
        * `LAST_OWNER_CANNOT_BE_DEACTIVATED` - A company must keep at least one active owner
        * `ONLY_TECHNICIANS_ARE_ASSIGNED` - Customer assignments apply to technicians only
        * `CANNOT_DEACTIVATE_SELF` - A user cannot deactivate their own account
        * `BUILDING_CUSTOMER_MISMATCH` - A building in a complex must share the complex's customer
        * `TERMINATION_REASON_REQUIRED` - A termination reason is required
        * `FIELD_REQUIRED_FOR_CUSTOMER_TYPE` - This customer type requires this field
        * `FIELD_NOT_VALID_FOR_CUSTOMER_TYPE` - This field does not apply to this customer type
        * `STATUS_NOT_USER_SELECTABLE` - This status is assigned by the system
        * `INVALID_CREDENTIALS` - E-mail or password is incorrect
        * `ACCOUNT_LOCKED` - Too many failed attempts; account is temporarily locked
        * `ACCOUNT_INACTIVE` - Account is not active
        * `TOKEN_INVALID` - Token is invalid or has already been used
        * `TOKEN_EXPIRED` - Token has expired
        * `EMAIL_NOT_VERIFIED` - E-mail address has not been verified
        * `FILE_TOO_LARGE` - File exceeds the 10 MB limit
        * `UNSUPPORTED_MIME_TYPE` - File type is not accepted
        * `UPLOAD_NOT_COMPLETED` - No uploaded object was found for this key
        * `ATTACHMENT_TARGET_MISMATCH` - Attachment does not belong to the record it is being linked to
    ErrorResponse:
      type: object
      description: 'The shape of every error response. There is no `message` field:
        the backend sends codes, and the words live in the client''s translation file,
        so wording changes in one place and adding a language does not touch the API.'
      properties:
        error:
          type: object
          properties:
            code:
              $ref: '#/components/schemas/ErrorCode'
            request_id:
              type: string
            details:
              type: array
              items:
                type: object
                properties:
                  field:
                    type: string
                  code:
                    type: string
                required:
                - code
          required:
          - code
          - request_id
      required:
      - error
  securitySchemes:
    jwtAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
